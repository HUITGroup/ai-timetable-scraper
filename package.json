{
  "name": "ai-timetable-scraper",
  "version": "0.1.0",
  "description": "Simple syllabus scraper with Puppeteer",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && npx tsc --build tsconfig.json",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@types/node-fetch": "^2.5.7",
    "node-fetch": "^2.6.1",
    "puppeteer": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "12",
    "@types/puppeteer": "^3.0.1",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.3.0",
    "typescript": "^4.0.2"
  }
}
